module.exports = class ShardReconnectReceive {
    constructor(client) {
        this.client = client
    }

    run(shard) {
       // console.log(`[SHARDING MANAGER] Relaunching shard ${shard}...`)
    }
}