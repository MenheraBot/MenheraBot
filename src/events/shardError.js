/* module.exports = class ShardErrorReceive {
    constructor(client) {
        this.client = client
    }

    run(err, shard) {
        console.log(`[SHARDING MANAGER] Error in Shard ${shard}! Error: ${err}`)
    }
} */